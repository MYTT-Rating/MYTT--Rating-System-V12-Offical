name: Audit Final Cleanup V28

on:
  push:
    branches:
      - final-cleanup-v28-20260821
    paths:
      - '.github/workflows/audit-final-cleanup-v28.yml'
      - '.github/scripts/audit_remaining_cleanup_v28.py'

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: final-cleanup-v28-20260821
          fetch-depth: 0
      - run: python .github/scripts/audit_remaining_cleanup_v28.py
      - run: |
          git config user.name "MYTT-Rating"
          git config user.email "hangtbabm@gmail.com"
          git add cleanup-audit-v28.txt
          git commit -m "Generate final cleanup audit"
          git push origin HEAD:final-cleanup-v28-20260821
# post-cleanup audit
